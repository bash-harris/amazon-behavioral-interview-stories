# Critique — Q68: "a time where you introduced a new process and why was it not already in place at your company?"

**Read against:** STYLE BAR = `AMAZON_INTERVIEW_PREPARATION.md` STORY 4. Sources checked: `AMAZON_INTERVIEW_PREPARATION.md` STORY 15 (lines 1331–1400), STORY 4 (lines 399–475), STORY 5 (line 551). Conflict excluded: `AMAZON_INTERVIEW_PREP.md` STORY 7. Reuse verified against Q34 (PASS).

```
VERDICT: PASS
SCORE: 8/10
BIGGEST GAP: The question is identical to Q34 and the answer overlaps it heavily — Flask origin, factory-floor constraint, 30 min → 8 s, ~1-in-5 → 0, ~200 lines, Webpack/CDN rejection, one-line manifest change — so the claimed "distinct re-frame" (release governance) is real but thin; the only genuinely new core element is elevating the 15-BOM regression gate from a Q34 follow-up into the process definition.
SPECIFIC FIX: Sharpen the governance reframe so it cannot be read as Q34 with a new opening: lead with a concrete release-control failure mode the gate/ordering/manifest prevents (a silently mis-ordered deploy that reached the floor), tie each of the three controls to that specific risk, and trim the duplicated build-mechanics recital to keep the governance half dominant.
Coverage: Invent and Simplify — all 5 strengths evidenced: generates a new method (manifest-driven automated release replaces hand packaging); improvement + scalable without complexity (~200 lines, no bundler, 4th tool = one-line manifest change); internal + external ideas (internal factory-floor constraint; external Webpack and CDN evaluated and rejected on fit); long-term customer impact (ordering correctness stops silent floor breakage; git-hash stamp makes every field file traceable); seeks out process improvement (proactively replaces the manual step). Zero concern indicators: does not accept the manual process, does not build a cumbersome one, does not look for a standard process to copy, names a better process rather than dwelling on difficulty.
Style match vs STORY 4: same — first person, concrete nouns (manifest ordering, base64 libs, git hash in the title, test gate), a real trade-off (~25 MB offline vs ~5 MB CDN), honest reflection (checksum/diff named, not built), correct length. Register is plain and believable.
Fabrication check: suspicious: minor — every number traces to single sources: 30 min → 8 s and ~1-in-5 → 0 (`AMAZON_INTERVIEW_PREPARATION.md` STORY 15 lines 1352, 1368); ~200 lines vs Webpack (1380); three-stage collect/process/assemble and CSS-before-HTML / JS dependency order / base64 (1360–1362); git commit hash in the HTML title (1364); test gate: open compiled HTML, run workflow, compare to server version on the 15-BOM suite, reject on failure (1392); CDN ~25 MB → ~5 MB (1396); Flask origin and factory constraint (STORY 4 lines 401, 407). The phrase "one core, many views" appears in neither bank (grepped both) — it is a coined descriptor attributed in provenance to STORY 5 line 551, which only documents the one-line manifest change; a minor label, not a metric. Conflict exclusion (`PREP.md` STORY 7: 150 MB, 2–3 days → 5 min) is correct.
```

## Notes

- Passes the REUSE RULE: it is not near-verbatim and it does adapt/reframe to the release-governance angle, so (d) is not triggered. But the reframe is the weakest part — a harsh reader will notice it is the same answer to the same wording as Q34.
- Duplicate routing is stated honestly; the underlying facts are all single-sourced and correct.
