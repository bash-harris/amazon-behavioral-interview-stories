# Critique — Q85 (round 2)

Question: "describe a time when you needed to deep dive to solve a problem"
Mapped LP(s): Dive Deep (primary); Are Right, A Lot (touch)

## What I checked

- **The round-1 fabrication is gone.** The old line "I made the change and re-ran the boards I had; the aspect-ratio bias was gone" does not appear anywhere. The core now states the opposite plainly: "The fix is identified and scoped, but I have not landed it" (para 5) and "That code is still on my to-build list; I did not make the change and I did not re-measure it." The "Have you fixed it?" follow-up answers "No, and I want to be exact." No completed-fix or measured-delta claim survives.
- **Mechanism still accurate.** 128×128 letterbox with constant gray `(114,114,114)`, SSIM over the full padded tile reduced by the mean, identical padding → local SSIM ≈ 1.0 → mean dragged toward "pass" in proportion to padded fraction; non-square = connectors/wide ICs → `BAR_RAISER_TECHNICAL_PREP.md` Finding 3, lines 158–164, 166. Bonus warts (`t1` recompute/"hack", unused `t3` deleted) → line 174. All match.
- **Two traps correctly refused.** It does not quote SSIM's weight (0.40 in BAR_RAISER line 164 vs the STORY 6 telling's raw 0.2 / CLAHE 0.3 / contrast 0.2 / edge 0.3 — different formulations), and it does not claim the "0.71 → 0.88" recall figure, which the source frames only as a hypothetical payoff of doing the work (lines 205–210, 356–360).
- **Nit, not a defect.** Provenance line 60 cites "critiques/Q85.md lines 10–13" for critic verification; that pointer is loose (the prior file is not line-numbered in the same frame), but it references real verification of `component_analysis.js:237`, not a story fact.

## Verdict

```
VERDICT: PASS
SCORE: 8/10
BIGGEST GAP: The answer is a diagnosis with no delivered change, so Dive Deep's "steps in and gets work done" rests on the scoping/precision of the proposed ROI fix rather than a shipped result — a weaker close than STORY 4's delivered artifact, though the honesty is exactly what the task demanded.
SPECIFIC FIX: Add one clause grounding "gets work done" in work actually completed — e.g. "the dive itself was the work: I read the scoring path end to end, reproduced the aspect-ratio dependence, and confirmed the bug is live at `:237` in every copy of the file" — so the indicator is evidenced by an action taken, not only by a plan.
Coverage: Dive Deep — stays connected to details ✓ (tile size, gray, SSIM mean, crop bounds); understands how systems work together ✓ (preprocess → SSIM map → mean → anomaly score); critically evaluates metrics/data ✓ (refused to trust plausible scores); asks good questions ✓ ("why does the score depend on crop shape?"); investigates/gets details ✓; gathers difficult info ✓ (traced a plausible number to its cause); steps in and gets work done ~ (dive done, fix scoped but not landed). No Dive Deep concern triggered. Are Right, A Lot (touch) — recognizes the initial belief was wrong on evidence ✓.
Style match vs STORY 4: same — first person, concrete, honest reflection; the unclaimed fix reads as engineering judgment, not a gap discovered by the interviewer.
Fabrication check: clean — no completed-fix claim remains; every number (128×128, gray 114, 2:1, the two warts, the to-build status, the refused recall delta) traces to BAR_RAISER Finding 3 / Part 5 and STORY 6 is left out to avoid the weight-value blend.
```

## Why it passes

1. The specific defect that failed round 1 — presenting designed work as built — is fully corrected and stated three times in the spoken core plus provenance.
2. The deep-dive itself (reading the scoring code, isolating padding as the shape-correlated step, naming the exact bounds to fix) is genuine Solve-the-problem work with the details named.
3. Conflicting tellings are excluded by an explicit single-telling note, so no number is blended.

## Residual risk

- A bar raiser may press "so what did you actually change?" The answer's defense is honest and prepared ("I won't present designed work as built"), which is the correct posture but means the story has no delivered-result payoff. That is acceptable for a Dive Deep question and is the trade the task asked for.
