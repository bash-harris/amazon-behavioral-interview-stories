# Q8 critique — "tell me about time where you completed a project on your own"

VERDICT: PASS
SCORE: 9/10
BIGGEST GAP: The single most Think-Big-forward line — "I should have designed for standalone from the start rather than retrofitting it out of a server build" — is uncited in Fact provenance and is lifted from `AMAZON_INTERVIEW_PREP.md` STORY 7 (line 304), the exact telling the answer says it is not using; and "it fit on email or a USB stick" contradicts the answer's own reflection that 25MB hits 20MB email caps.
SPECIFIC FIX: Cite `AMAZON_INTERVIEW_PREP.md:304` as the source for the architectural-lesson sentence (it is a real reflection, so keep it — just source it), or cut it; and change "large, but it fit on email or a USB stick" to "large, but it distributed by USB drive and by email where attachment limits allowed," so the result line and the reflection no longer fight each other.
Coverage: Think Big strength indicators —
- identifies bold, rational, defensible direction → HIT (delete the server; ship one offline HTML file).
- takes risks in pursuit of an idea → HIT (base64 WASM single-file architecture).
- carefully reviews existing processes → HIT (dependency audit 8/3/1/0.5MB; manual-vs-automated compile 30min·1-in-5 → 8s·0).
- works around limitations/challenges → HIT (WASM loader rewrite to data URL/ArrayBuffer; localStorage 5MB quota + GC; no CDN).
- thinks about a problem from a new perspective → HIT (offline-first single artifact the user owns instead of server-first).
Think Big concerns → NONE triggered (not day-to-day-only; bold direction present; big picture present).
Secondary: Ownership "sees things through to completion" HIT; Deliver Results "well-executed work" HIT.
Style match vs STORY 4: same — first person, real numbers, "The hard part was OpenCV.js" / "The compile step showed the value" beats mirror the bar; the mid-section enumeration is no more list-like than STORY 4's own compile_tool sentence.
Fabrication check: clean — every number traces to STORY 4 or STORY 15 (12.5MB libs, ~25MB file, ~5s vs ~2s, 30min→8s, ~1-in-5→0, 15 BOMs + 10 images, ~200 lines); no 150MB / 5-min / $500-month blend; no Tab Scroller reference anywhere. Only defect is the uncited (but real) PREP.md:304 reflection noted above.
Note: "completed on your own" is answered head-on, and completion is defensible — STORY 4 documents the file distributed by email/USB and Double-click use in engineers' hands, not the unfinished Tab Scroller.
