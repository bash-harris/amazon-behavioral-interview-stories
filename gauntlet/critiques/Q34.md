# Critique — Q34: "tell me about a time where you introduced a new process and why was it not already in place at your company?"

**Read against:** STYLE BAR = `AMAZON_INTERVIEW_PREPARATION.md` STORY 4. Sources checked: `AMAZON_INTERVIEW_PREPARATION.md` STORY 15 (lines 1331–1400), STORY 4 (lines 399–475), STORY 5 (line 551). Conflicting telling excluded: `AMAZON_INTERVIEW_PREP.md` STORY 7.

```
VERDICT: PASS
SCORE: 9/10
BIGGEST GAP: All five Invent-and-Simplify strengths are hit and every number is single-sourced, but the "why not already in place" half rests on a logical inference (the requirement was new) rather than any corroborating organizational signal, and the "wide range of sources" is carried by only two names (Webpack, CDN).
SPECIFIC FIX: Add one concrete, sourced organizational beat to the "why it wasn't there" answer — e.g. that the suite was built by one developer who was shipping by hand and nobody owned build tooling — so the reason reads as a live constraint, not a tidy deduction. Keep it qualitative unless a source states it; do not invent a headcount or a date.
Coverage: Invent and Simplify — all 5 strengths evidenced: generates a new method (automated manifest-driven build replaces manual packaging, core paras 3–4); identifies improvement + scalable without complexity (~200 lines, no bundler, a 4th tool is a one-line manifest change); actively gathers internal + external ideas (internal factory-floor constraint; external Webpack and CDN both evaluated and rejected on fit, core para 4); long-term customer impact (inlining order correctness + git-hash traceability for the floor); seeks out process improvement (replaces the manual step rather than accepting it). Zero concern indicators: does not accept the existing process, does not build a cumbersome one, does not look for a standard tool to copy, and names a better process instead of dwelling on difficulty.
Style match vs STORY 4: same — first person, concrete nouns (manifest order, base64 libs, git hash stamped in the title), a real trade-off (~25 MB offline vs ~5 MB CDN), and an honest reflection (checksum/diff named and not built). Correct length. Two rhetorical openers ("the more interesting half of the question") sit a notch above STORY 4's plain register but do not break belief.
Fabrication check: clean — 30 min → 8 s and ~1-in-5 → 0 (`AMAZON_INTERVIEW_PREPARATION.md` STORY 15 lines 1352, 1368); ~200-line Python script vs Webpack (1380); three-stage collect/process/assemble and CSS-before-HTML, JS dependency order, base64/WASM loader (1360–1362); git commit hash in the HTML title (1364); CDN 25 MB → ~5 MB (1396); ~25 MB artifact and Flask origin (STORY 4 lines 423, 401); 4th tool = one-line manifest change (STORY 5 line 551); test harness 15 BOMs + 10 board images, reconciliation exact / alignment ≤1 px / classifications identical (STORY 4 line 463). Correctly excludes the conflicting `PREP.md` STORY 7 telling (150 MB; 2–3 days → 5 min). "Deploying meant pointing someone at a URL" is a gloss on the Flask origin, not a conflicting metric.
```

## Notes

- The exact question is answered head-on: the answer names the introduced process (`compile_tool.py`) and dedicates its opening half to *why it wasn't already there* (no packaging step while it was a Flask server; the single-file requirement appeared only when the factory-floor constraint did). This is the check the grader asked to confirm — it holds.
- Distinctness from Q31 is stated and true: same artifact, different subject (process introduction vs speed-to-deadline).
- The one thin strength is "actively gathers ideas from a wide range of internal and external sources." Two named external options plus the internal customer constraint is enough to avoid the "only internal" concern, but it is the least load-bearing bullet; the fix above does not require new facts.
