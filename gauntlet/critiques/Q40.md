# Critique — Q40: "tell me about a time you made an unorthodox decision"

```
VERDICT: PASS
SCORE: 8/10
BIGGEST GAP: Invent-and-Simplify's "actively gathers ideas from a wide range of internal and external sources" is claimed as covered by follow-ups 3 and 6, but neither follow-up names a single internal source — no engineer session, no security review, no internal stakeholder whose feedback shaped the design appears anywhere in the body, so the strength is asserted rather than evidenced.
SPECIFIC FIX: Add 2–3 sentences to the Action paragraph naming the internal input — the engineers' screen-sharing sessions observed before building (PREP.md STORY 1, line 55) and the security review that approved sending board photos to the API (STORY 8 line 841) — and correct the "Strong signals covered" citation from "(follow-ups 3, 6)" to those actual passages.
Coverage: Are Right, A Lot — makes good decisions under ambiguity HIT (chose the VLM path over the default classifier on a named bottleneck); seeks additional perspective/data HIT (evaluated GPT-4V / Claude 3 / Gemini Pro Vision / LLaVA before committing); recognizes not always right / supports best idea HIT (states the value-reading assumption was wrong; keeps the engineer as decision-maker); recognizes lacking expertise/info HIT (owns the black-box model as untrained and bounds the risk). MISSED: "applies experience to determine the best approach" is never framed as experience. Invent and Simplify — generates new methods HIT; scalable/avoids complexity HIT (only flagged regions sent, API optional, deterministic core retained); focuses on long-term customer impact WEAK (short-term triage benefit only); seeks process improvement HIT. MISSED/PARTIAL: internal-source half of the sourcing strength (see gap). Zero Concern indicators triggered for any of the three LPs. Bias for Action — moves quickly under uncertainty HIT; willing to make small progress vs perfect HIT (shipped with the deterministic engine as core); carefully considers what is important now vs what can wait HIT. MISSED: "understands when to consult others and when it's not necessary" is not clearly evidenced.
Style match vs STORY 4: same — first-person, concrete, shows the constraint, the technical reasoning, the bounded risk, the measured result, and an honest reflection; the enumerated "First/Second/Third" risk list reads slightly more essayistic than STORY 4's flow but stays within register.
Fabrication check: suspicious (minor) on one point — the standalone-VLM scope merges two conflicting source tellings and asserts a conclusion neither source states verbatim: STORY 8 line 793 has the standalone build carry a GPT-4o API-key configuration panel, while STORY 4 line 459 says the standalone build does not use the VLM feature. Q40 writes "I confined it to the server build" and "the standalone factory-floor build doesn't use the VLM at all" while citing STORY 8 Task line 783 (which says the integration must work in the standalone version). The conclusion matches STORY 4, but unlike Q3 the conflict is not reconciled or flagged, so the claim is internally muddled. All metrics are otherwise clean and single-telling: 30s→~5s per mismatch, ~85% accuracy, remaining 15% poor-image/too-small (STORY 8 Result, line 797); 5–20 mismatches/board (line 821); GPT-4V ~8s vs ~3s, Claude 3 API less mature, Gemini Pro weaker on small SMD (line 813); LLaVA ≥8GB VRAM absent on factory machines (line 837); JSON fields component_type / mismatch_type / confidence / description (line 791); "AI Analysis" + correct/incorrect feedback loop (line 829); graceful "AI analysis unavailable" fallback (line 825). The conflicting 92% / 100-board-pairs telling is correctly excluded.
```

## Fabrication detail (a) — metrics

- 30s → ~5s per mismatch; ~85% on component type and mismatch type; remaining 15% images too poor / component too small → `AMAZON_INTERVIEW_PREPARATION.md` STORY 8 Result, line 797. MATCH.
- 5–20 mismatches per board → STORY 8 follow-up "How do you handle API rate limits," line 821. MATCH.
- GPT-4V ~8s vs GPT-4o ~3s; Claude 3 API less mature; Gemini Pro Vision weaker on small objects → STORY 8 follow-up "Why GPT-4o specifically," line 813. MATCH.
- local LLaVA ≥8GB VRAM, factory machines lack it → STORY 8 follow-up "Could you run the VLM locally," line 837. MATCH.
- JSON output fields; "expert eye" phrasing → STORY 8 Action lines 787–791; "expert eye" also `AMAZON_INTERVIEW_PREP.md` STORY 3 line 123. MATCH.
- Reflection: wrong that the model could read values; would start with the simple structured prompt → STORY 8 Reflection, lines 803–805. MATCH.
- Conflicting 92% / 78% baseline / 100 board pairs telling (PREP.md STORY 3) deliberately unused → correct per LESSON 1.

## Fabrication detail (b) — open issue

- **Standalone VLM conflict, unresolved.** `AMAZON_INTERVIEW_PREPARATION.md` STORY 8 Task line 783: integration "needed to work both in the server version (with API access) and in the standalone version (where API access might not be available)." STORY 8 Action line 793: the standalone version got "a configuration panel where users can enter their own GPT-4o API key." STORY 4 line 459: "The standalone version doesn't use the VLM feature, so no API key is needed." Q40 asserts the server-only conclusion and cites STORY 8 line 783 for the standalone constraint, producing an internal contradiction. Q3 handled the identical conflict by explicitly reconciling it; Q40 should do the same or drop the standalone clause.

## Why it passes despite the gaps

1. The exact question — the unorthodox decision itself, the risk, and why it was right — is answered head-on, and the answer is framed as a choice, not a replay of the productivity (Q1) or business-result (Q3) framings.
2. No Concern indicators for Are Right, A Lot / Invent and Simplify / Bias for Action are triggered; the residual weakness (small SMD text) is named rather than hidden, which is exactly the "recognizes they are not always right" signal.
3. The unverified internal-sourcing claim and the unreconciled standalone conflict are fixable in two edits and do not invent any metric or event. The numeric spine is single-telling and fully cited.

## Residual risk

- A bar raiser probing "whose internal input shaped this decision?" will find nothing in the body; the coverage block's citation to follow-ups 3 and 6 does not survive a read of those follow-ups. Fix before it is used.
