# Critique — Q91

Question: "a time where you received critical feedback, how did you implement it?"
Mapped LP(s): Earn Trust (primary); Learn and Be Curious

## What I checked

- **Parser facts trace to STORY 1.** `AMAZON_INTERVIEW_PREPARATION.md` STORY 1: variant references `R1||R2` / `R1/R2`, initially treated as invalid and silently dropped, engineer said the system should match either reference (line 89); variant splitter normalizing `||` and `/`, extracting individual references as valid match targets (line 89); test matrix `R1||R2`, `R1/R2`, `R1||R2||R3`, `R1/R2/R3`, `||R1`, `R1/` (line 131); 15 representative BOMs run after every change (lines 93, 151); default degradation — extract what matches the standard pattern, skip unknown without crashing (line 135). All match.
- **The debug view is STORY 11, same tool, consistent.** `AMAZON_INTERVIEW_PREPARATION.md` STORY 11 line 1049: `?debug=true` shows the raw BOM entry, the parsed reference "(after MP_ stripping, hyperlink extraction, variant splitting)," the layout reference, the result, and the rules — exactly the transparency the answer claims. The combination is deliberate and stated, not a blend of conflicting tellings.
- **Mild overstatement, not fabrication.** The core says "I stopped letting unrecognized formats vanish. The parser's default is now to extract whatever matches the standard reference pattern and skip the rest without crashing." Per line 135 that skip-without-crashing *is* the documented default, and it still silently skips unknown formats; what changed is the variant splitter plus the debug view making skips visible. The answer partly corrects this in its own follow-up ("a new format shows up as a visible skipped entry rather than a silent loss"), so the net claim is defensible but the "stopped letting them vanish" phrasing overstates a behavior the source describes as intentional degradation.

## Verdict

```
VERDICT: PASS
SCORE: 8/10
BIGGEST GAP: "I stopped letting unrecognized formats vanish" overstates the change — STORY 1 documents skip-unknown-without-crashing as the deliberate default behavior, and what the splitter plus debug view actually fixed was the silent drop of the variant class and the visibility of remaining skips.
SPECIFIC FIX: Reword to "I fixed the class of silent drop I was shown — the variant notation — and made the remaining skips visible in the debug view, so a new format shows up as a skipped entry the engineer can flag instead of a component that quietly disappears," matching STORY 1 line 135.
Coverage: Earn Trust — seeks out and accepts feedback ✓; takes responsibility for shortfalls ✓ ("I treated it as invalid and silently dropped it"); represents data transparently ✓ (debug view exposes parsed reference after each preprocessing step); treats others' ideas with respect ✓ (implements the engineer's semantics and pins their case as a fixture); honors commitments ✓ (fix verified in the 15-BOM suite). Zero concerns triggered. Learn and Be Curious — seeks/embraces feedback ✓; reacts to negatives by improving for the future ✓ (generalizes past the reported string); discusses lessons from setbacks ✓ ("silent data loss is more dangerous than a crash"); asks "I wonder what would happen if…" ✓ (adversarial test matrix). Zero concerns triggered.
Style match vs STORY 4: same — first person, concrete, constraint → reasoning → trade-off → verified result → honest reflection.
Fabrication check: clean — all facts trace to STORY 1 lines 89–151; the `?debug=true` view is STORY 11 line 1049 (same tool, stated as a deliberate combination); the conflicting four-edge-case "15% → under 1%" telling is excluded. No invented number, no mislabeled work; only the degradation phrasing needs tightening.
```

## Residual risk

- The event's mapped LPs in the source (Customer Obsession, Ownership, Dive Deep, Insist on Highest Standards, Deliver Results) differ from the question's (Earn Trust, Learn and Be Curious); the behaviors are evidenced, but the answer relies on the reviewer reading behavior rather than the doc's LP tag — which is legitimate but worth knowing.
