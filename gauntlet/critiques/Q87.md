# Critique — Q87

Question: "describe a time when you optimized the performance of an application"
Mapped LP(s): Dive Deep (primary); Invent and Simplify; Deliver Results

## What I checked

- **Every figure matches the source table, unblended.** `IMPLEMENTATION_GUIDE.md` "Expected Performance Gains" (lines 628-638): simple queries 2s / 5,000 tokens → 10ms / 0 tokens; complex 50-tab 8s / 50,000 → 3s / 3,000; extraction 5s → 0.5s; cost $0.50 → $0.03; offline ❌ → ✅ 70%. Q87 reproduces each with the same values. `smartPreFilter` zero-token / <1ms, top 30%, min 5 / max 30, and the 50→8 example → lines 29-119, 111, 567. `parallelExtraction` batches of 10, "Completed 10/10 in 523ms," <1s vs 5+s → lines 136-166, 579-581. Triplet compression ~80% → line 279. "10x faster / 95% cheaper" → `EXTENSION_SUMMARY.md`. No conflicting telling merged.
- **Implementation verification is real.** I resolved the cited anchors in the actual file: `background.js:924` `function smartPreFilter`, `:1005` `async function parallelExtraction`, `:1041` `function tryRuleBasedGrouping`, `:1139` `function compressToTriplets`, with call sites at `:2817, :2957` (rule), `:2993` (pre-filter), `:3028` (parallel), `:3033` (triplets). So the mechanisms are genuinely shipped.
- **The projected-vs-measured line is held.** The honesty note and core both state the table is titled "Expected Performance Gains" and the figures are the guide's projected gains, not re-benchmarked. This is the correct handling of LESSONS #3. One small drift: para 5's Deliver Results bullet says 70% of queries "now work" with the network off (present tense), which slightly outruns the projected label — but the core sentence and note bracket it clearly.

## Verdict

```
VERDICT: PASS
SCORE: 9/10
BIGGEST GAP: The 70%-offline capability is stated in the present tense in the Deliver Results line ("now work"), which reads as measured even though the source lists it in the projected-gains table.
SPECIFIC FIX: Change "roughly 70% of queries now work with the network off" to "the guide projects roughly 70% of queries working offline once the rule path handles the predictable commands" so every quantity in the answer carries the same projected framing.
Coverage: Dive Deep — stays connected to per-stage details, critically evaluates metrics (refuses to blame the model), investigates to find the real bottleneck, understands system stages — all evidenced. Invent and Simplify — generates new methods, scalable improvements that avoid complexity, gathers ideas from internal/external (Gemini + Ollama) — evidenced. Deliver Results — overcomes obstacles, focuses on the most important result (common case instant), delivers a new capability — evidenced. No concern triggered for any mapped LP.
Style match vs STORY 4: same
Fabrication check: clean — all speed/cost/token figures come only from the IMPLEMENTATION_GUIDE "Expected Performance Gains" section and are labelled projected; the four functions and their call sites are verified present in `background.js` (924/1005/1041/1139; calls 2817/2957/2993/3028/3033); no measured-result number is claimed.
```
