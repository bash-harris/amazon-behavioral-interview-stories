# Critique — Q90

Question: "a time when you optimized the performance of an application"
Mapped LP(s): Dive Deep (primary); Invent and Simplify; Deliver Results

## What I checked

- **Every number traces to the PREPARATION.md telling.** `AMAZON_INTERVIEW_PREPARATION.md` STORY 3: accuracy above 90% and inference under 100ms criteria (line 301), restated at STORY 14 line 1273; ResNet/EfficientNet/ViT evaluation and EfficientNetV2S choice (lines 305–307); S≈21M, M≈54M, L≈119M params, 224×224, S trains ~3x faster than L, S-vs-L accuracy difference <2% (line 335); ~8 billion FLOPs, ~45ms CPU, <5ms GPU, WebGL "close to GPU performance" (line 375); 94% on 500 held-out images and per-class recall 92/96/98 (line 319); retrain ~30 min on one GPU (line 321). The conflicting `AMAZON_INTERVIEW_PREP.md` STORY 6 "85% → 93%" and STORY 5 "60ms" figures are held out.
- **Projected vs measured is labeled.** Core: "the sub-5ms and WebGL figures are the documented performance characteristics of the hardware paths, not a benchmark I re-ran." Follow-up: "No — I present those as the documented performance characteristics ... not as benchmarks I ran." The next-step parallelization is likewise labeled: "the source projects about a 3.5x speedup from Web Workers, which I'd treat as projected until I measured it" (STORY 6 line 665). This satisfies LESSONS #3.
- **One cross-story join.** The OpenCV.js-load bottleneck is documented for the 4-signal comparison pipeline (STORY 6 line 661; BAR_RAISER line 332), and the answer applies it to "this pipeline." It is the same app and consistent, but it is a join, not a single-story fact.
- **The question is answered, but as a sizing decision.** There is no before/after on the same app: the "optimization" is choosing S over M/L and the measured anchor is 45ms against the <100ms target. The answer anticipates the obvious probe ("How do you know you optimized anything, versus just picked a smaller model?") and answers it honestly.

## Verdict

```
VERDICT: PASS
SCORE: 8/10
BIGGEST GAP: There is no true measured before/after — the story is a model-size selection held against a latency budget rather than a profile-and-optimize loop, and the OpenCV.js "load is the bottleneck" fact is joined in from the 4-signal pipeline rather than this inference path.
SPECIFIC FIX: State the pre-decision baseline on the page (e.g. "the initial large-model path could not be run inside the interactive budget; S made it runnable at 45ms") or, if no such baseline exists, label the sizing explicitly as "a design-time budget decision, not a profile-driven speedup" so the answer never reads as a measured optimization that did not happen.
Coverage: Dive Deep — stays connected to details ✓ (param counts, 224×224, ~8B FLOPs, 45-vs-100ms); understands how systems work together ✓ (params/input → convolutional FLOPs → CPU latency and WebGL); critically evaluates metrics/data ✓ (measured <2% S-vs-L delta; recall held); asks good questions ✓ (reframed to "what fits 100ms?"); steps in and gets work done ✓. Invent and Simplify — new method ✓ (sized architecture to the target instead of defaulting to largest); scalable/non-complex ✓ (30-min retrain); long-term customer impact ✓ (interactive budget). Deliver Results — meets the bar with quality ✓ (45ms vs <100ms, recall >90%); overcomes/persists ✓ (browser-only, no server). Zero concerns triggered.
Style match vs STORY 4: same — first person, concrete, constraint → reasoning → trade-off → measured result → honest reflection.
Fabrication check: clean — no measured number is invented; sub-5ms and WebGL are explicitly labeled as documented hardware characteristics, not benchmarks; the Web Worker speedup is labeled projected; conflicting 85→93% and 60ms tellings are excluded. Watch item: the OpenCV.js-load bottleneck is a consistent cross-story join (same tool), not a blend of conflicting numbers.
```

## Residual risk

- Because Q87 answers the same question from the Tab Scroller, the bar raiser may compare the two; Q90's weaker point is the absence of a numeric before/after, which the fix above would resolve.
